# M25C Southern Europe Grade-A Promotion Ship Manifest

## User goal

Promote UN M49 region 039 (Southern Europe) with an exact-date, source-pinned,
four-layer Grade-A evidence packet while preserving the non-public worldwide
certification boundary.

## Changed files

- `research/start-dates/1444-global-v1/regional-packets/039-southern-europe-2026-08-15.json`
- `research/start-dates/1444-global-v1/regional-packets/assets/039/boundaries.geojson`
- `research/start-dates/1444-global-v1/regional-packets/assets/039/polity-masks.geojson`
- `research/start-dates/1444-global-v1/regional-packets/README.md`
- `scripts/generate-m25c-region-039-packet.py`
- `tasks/history.md`
- `tasks/m25c-region-039-ship-manifest.md`
- `tasks/roadmap.md`
- `tasks/todo.md`
- `tests/test_m25c_global_certification.py`

## Per-file purpose

- The region-039 packet is the accepted, source-pinned four-layer Grade-A
  replacement for every Southern Europe assignment.
- `boundaries.geojson` and `polity-masks.geojson` preserve the checked
  Portugal–Castile frontier and its independently cited coverage masks.
- The regional packet README indexes the new dated packet and documents the
  country-based M49 treatment.
- The generator reproduces the packet, source pins, capital gates, assertions,
  and derived assets while failing closed on count or spatial drift.
- The certification test qualifies the real packet and fixes its counts,
  accepted render digest, exact-date labels, and absence of provisional sources.
- Todo, roadmap, and history advance M25C from three to four promoted regions.
- This manifest records the exact shipping boundary, verification, risks,
  rollback, and next route.

## Outcome

The packet replaces all 464 region-039 provisional assignments and promotes all
four coverage rows to gap-free Grade A. It replaces 23 polity records, pins 18
complete reviewed source records and exact locators, adds seven capital features
and 29 executable assertions, and checksum-pins two Portugal–Castile frontier
assets. The `1444-11-11` interpretation explicitly postdates the 10 November
Battle of Varna. The country-based M49 sheet requires no correction.

The worldwide provisional pass now contains 16 Grade-A rows. Eighteen regions
and 72 Grade-A rows still block ordinary review acceptance, certification,
runtime publication, and demo promotion.

## User-goal mapping

"Exact-date" is enforced by the start date, date-applicable sources, and the
post-Varna Ottoman label. "Source-pinned" is enforced by SHA-256 hashes over
each complete canonical source record and exact locator. "Four-layer" is
enforced by four gap-free Grade-A rows and matching executed geometry,
politics, hierarchy, and gazetteer-relationship gates. Promotion is exercised
inside the worldwide provisional merge without weakening public-release gates.

## Tests run

- `uv run pytest -q tests/test_m25c_global_certification.py` — 26 passed.
- `uv run pytest -q` — 368 passed in 41.77 seconds with no test warnings or
  failures.
- `uv run python scripts/generate-m25c-provisional-pass.py ... --qa` — complete
  22-region assembly passed with zero errors and 12 expected provisional
  evidence/review warnings; coverage is exactly 16 A, 18 B, and 54 C rows.
- `uv run python scripts/generate-m25c-region-039-packet.py` plus `cmp` — the
  packet and both derived GeoJSON assets regenerated byte-identically.
- Southern Europe review-sheet render and visual inspection; accepted SHA-256
  `ee88cd687939060294c5a3e75ca392435245a605d2dc2ab6e3fbf0aff96f0f66`.
- `jq empty` validated the packet and both GeoJSON assets; Python `py_compile`,
  `git diff --check`, trailing-whitespace scan, and changed-file private-key /
  provider-token scan passed.

## Skipped tests

Ordinary human acceptance, runtime compilation, certification, publication,
and deployment remain out of scope because 18 regional packets are incomplete
and those gates must reject this provisional lineage. No repository lint,
typecheck, or standalone build command is configured in `pyproject.toml`; Python
compilation and the complete executable suite cover the changed generator and
packet contract.

## Adversarial review

The domain-specific equivalent review combined changed-file self-review, the
packet qualifier's failure-oriented tests, full-world assembly, deterministic
regeneration, and visual inspection. It exercised tampered canonical source
pins, missing/checksum-invalid or escaping assets, duplicate capitals, spatial
assertions, authoritative source/polity replacement, and the unchanged
provisional certification boundary. Review found two concrete provenance
defects during development: missing source-derived boundary artifacts and
duplicate derived-artifact IDs. Both were fixed by adding checksum-pinned
boundary/coverage assets and assigning unique artifact ownership across the two
independent sources. The final worldwide QA has zero errors.

## Residual risk

Historical interpretation is bounded by the cited sources and accepted M23 r2
fabric. The checked hard frontier is a source-qualified shared-fabric segment,
not a newly digitized full Iberian boundary. The remaining risk is historical
granularity within the accepted scaffold; it will be noticed through later
regional research or visual review, not through runtime publication because the
worldwide lineage remains non-certifiable.

## Rollback note

Revert the shipping commit to remove this packet, its generator, tests, assets,
and task records and restore the prior three-region provisional state. No public
runtime or deployment requires rollback.

## Next command

`$exec` — promote M49 region 145 (Western Asia) with the next exact-date,
source-pinned four-layer packet.
