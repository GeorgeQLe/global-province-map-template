# M25C Western Africa Grade-A Promotion Ship Manifest

## User goal

Promote Western Africa (M49 011), the largest remaining Africa/Americas region
at 641 assignments, with an exact-date, source-pinned, four-layer Grade-A
packet while preserving the non-public worldwide certification boundary.

## Changed files

- `research/start-dates/1444-global-v1/regional-packets/011-western-africa-2026-08-16.json`
- `research/start-dates/1444-global-v1/regional-packets/README.md`
- `scripts/generate-m25c-region-011-packet.py`
- `tasks/history.md`
- `tasks/m25c-region-011-ship-manifest.md`
- `tasks/roadmap.md`
- `tasks/todo.md`
- `tests/test_m25c_global_certification.py`

## Per-file purpose

- The dated regional packet carries the complete evidence, assignment,
  gazetteer, assertion, M49-correction, and accepted-render records.
- The packet generator deterministically rebuilds that artifact from the
  accepted baseline and tracked Natural Earth country fabric.
- The regional README indexes the accepted packet and summarizes its scope.
- Todo, roadmap, and history advance M25C from thirteen to fourteen promoted
  regions and retain the non-public certification boundary.
- The focused certification test fixes packet counts, required and forbidden
  actors, render digest, source-pin qualification, and both M49 corrections.
- This manifest records the exact shipping, verification, risk, rollback, and
  next-work boundary.

## User-goal mapping

"Western Africa (M49 011)" is enforced by exact region and assignment-count
checks. "641 assignments" is enforced by deterministic complete replacement
and unique province-ID assertions. Promotion is enforced by four gap-free
Grade-A coverage rows, canonical source pins, accepted visual review, and
executable spatial assertions. The worldwide lineage remains provisional,
non-public, and non-certifiable until every regional row is Grade A.

## Outcome

The packet replaces all 641 provisional assignments with 15 conservative
political or community fabrics for exactly `1444-11-11`. Eight pinned sources,
eight checked centers, and 32 assertions close all four Grade-A rows. The sheet
keeps the declining Mali Empire, Tuareg control of Timbuktu and Gao,
pre-imperial Songhai, and the distinct savanna, forest, coastal, and island
political fabrics visible without projecting modern borders backward.

The country-based M49 audit corrects one Algerian location to Northern Africa
`015` and one Cameroonian location to Middle Africa `017`. Fourteen regions are
now promoted; eight regions and 32 Grade-A rows remain.

## Verification boundary

The generator fixes assignment scope, actor coverage, canonical source pins,
checked-site containment, M49 corrections, packet counts, and deterministic
JSON. Whole-world provisional assembly, QA, rendered-sheet inspection, focused
and complete tests, package build, compilation, and diff checks form the
shipping gate. Ordinary human acceptance, runtime certification, publication,
and deployment remain unavailable until all 22 regions are Grade A.

## Tests run

- Deterministic generation produced 641 assignments, 15 polities, eight
  sources, 32 assertions, eight build features, no hard-frontier asset, and two
  M49 corrections; clean regeneration matched byte-for-byte.
- Complete 22-region assembly and provisional QA passed with zero errors, 12
  expected non-public warnings, 56 Grade-A rows, and exactly eight incomplete
  regions.
- The deterministic region 011 review sheet was visually inspected and matched
  SHA-256 `b799182c56dbec98d6d45f31bd8ce7380697f3c66745502fc94c8247677c4150`.
- Focused M25C certification tests passed all 37 cases, and the complete
  repository suite passed all 379 tests on the final diff. Package build,
  Python compilation, canonical pin validation, deterministic regeneration,
  and `git diff --check` passed.

## Skipped tests

Ordinary human acceptance, runtime compilation, worldwide certification,
publication, deployment, and production smoke testing remain intentionally
unavailable while eight regional packets are incomplete. They are downstream
gates, not executable validation for this non-public regional promotion. No
standalone lint or typecheck command is configured in `pyproject.toml`.

## Adversarial review

No configured `quality-sweep` or `expert-review` lane exists, so the equivalent
targeted review checked partial replacement, invalid canonical pins, duplicate
IDs, date-inapplicable evidence, unsupported hard boundaries, missing
four-layer coverage, checked-site containment, surviving modern scaffold
actors, country-based M49 leakage, render drift, and changes to the worldwide
certification boundary. The fail-closed qualifier, focused tamper contracts,
clean worldwide QA, deterministic regeneration, source and credential scan,
visual inspection, and complete test suite found no remaining executable
issue. The review identified and fixed two leaked country assignments by
moving Algeria to region `015` and Cameroon to region `017`.

## Residual risk

The evidence does not support exact local borders across much of Western
Africa in 1444. The packet therefore uses coarse political fields and 0.35
uncertainty outside documented states and checked centers. These are bounded
replacements for modern scaffold actors, not claims of uniform sovereignty or
immutable cultural boundaries.

## Rollback note

Revert the promotion changes to remove the packet, generator, test, and task
records. No public runtime or deployment requires rollback.

## Next command

`$exec` — promote Central America (M49 013), now the largest remaining
Africa/Americas region at 605 assignments.
