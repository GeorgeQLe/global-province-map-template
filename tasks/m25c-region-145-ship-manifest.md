# M25C Western Asia Grade-A Promotion Ship Manifest

## User goal

Promote UN M49 region 145 (Western Asia) with an exact-date, source-pinned,
four-layer Grade-A evidence packet while preserving the non-public worldwide
certification boundary.

## Changed files

- `research/start-dates/1444-global-v1/regional-packets/145-western-asia-2026-08-15.json`
- `research/start-dates/1444-global-v1/regional-packets/assets/145/boundaries.geojson`
- `research/start-dates/1444-global-v1/regional-packets/assets/145/polity-masks.geojson`
- `research/start-dates/1444-global-v1/regional-packets/README.md`
- `scripts/generate-m25c-region-145-packet.py`
- `scripts/generate-m25c-provisional-pass.py`
- `tasks/history.md`
- `tasks/m25c-region-145-ship-manifest.md`
- `tasks/roadmap.md`
- `tasks/todo.md`
- `tests/test_m25c_global_certification.py`

## Per-file purpose

- The dated packet and its two assets are the accepted Western Asia evidence,
  checked frontier, and independent polity-mask record.
- The region-145 generator reproduces the packet, source pins, exact-date actor
  corrections, capital gates, assertions, and derived assets while failing
  closed on scope or spatial drift.
- The provisional-pass generator preserves capitals, aliases, sources, and
  relationships when a polity appears in more than one regional packet.
- The regional README indexes the packet; todo, roadmap, and history advance
  M25C to five promoted regions; this manifest records the shipping boundary.
- The certification suite qualifies the real packet and prevents regression in
  its counts, source lineage, placeholder removal, review digest, and
  cross-region polity merging.

## Outcome

The packet replaces all 768 region-145 provisional assignments and promotes
all four coverage rows to gap-free Grade A. It supplies eight polity records,
pins 11 complete reviewed sources and exact locators, adds seven capital
features and 29 executable assertions, and checksum-pins two Ottoman-Qara
Qoyunlu frontier assets. The `1444-11-11` interpretation explicitly postdates
the 10 November Battle of Varna.

The packet removes modern Northern Cyprus and Palestine actors, repairs one
Muscovy-tagged Azerbaijan cell, and replaces all 371 uncurated Arabian cells:
74 resolve to the dated Rasulid-Yemen sheet and 297 to explicitly fragmented
local Arabian polities. No geographic M49 correction applies.

The worldwide provisional pass now contains 20 Grade-A rows. Seventeen regions
and 68 Grade-A rows still block ordinary review acceptance, certification,
runtime publication, and demo promotion.

## User-goal mapping

"Exact-date" is enforced by the start date, date-applicable records, and the
post-Varna Ottoman label. "Source-pinned" is enforced by SHA-256 hashes over
each complete canonical source record plus its exact locator. "Four-layer" is
enforced by four gap-free Grade-A rows and executed geometry, politics,
hierarchy, and gazetteer-relationship gates. Promotion is exercised inside the
worldwide provisional merge without weakening public-release gates.

## Tests run

- Deterministic packet generation: 768 assignments, eight polities, 11 sources,
  29 assertions, seven build features, two derived files, and zero M49
  corrections.
- Complete 22-region provisional assembly and QA: zero errors, 12 expected
  provisional evidence/review warnings, and coverage exactly 20 A, 17 B, and
  51 C rows.
- Western Asia review-sheet render and visual inspection; accepted SHA-256
  `5db0275d6156cd6188bb85abcadc28811f7a1b2452efcd2e5b5dc3add93afcac`.
- `uv run pytest -q tests/test_m25c_global_certification.py` — 28 passed.
- `uv run pytest -q` — 370 passed in 79.68 seconds.
- `uv build` — source distribution and wheel built successfully without build
  warnings.
- Generator output and both derived assets regenerated byte-identically; JSON
  parsing, Python compilation, `git diff --check`, trailing-whitespace, and
  changed-file credential scans passed.

## Skipped tests

- Ordinary human acceptance, runtime compilation, certification, publication,
  and deployment remain out of scope because 17 regional packets are still
  incomplete and the provisional lineage must reject those paths.
- No repository lint or static-type command is configured in `pyproject.toml`;
  Python compilation and the executable test suite cover the changed scripts.

## Adversarial review

Full-world assembly exposed a cross-region merger defect: a later packet for a
shared polity discarded an earlier packet's checked capital. The merger now
unions aliases, capitals, sources, and keyed relationships while retaining the
incoming canonical polity record; a regression test fixes that behavior. The
packet qualifier still recomputes every canonical source pin and validates
asset containment, checksums, independent corroboration, exact-date bounds,
complete assignment replacement, and all four Grade-A rows.

## Residual risk

Historical interpretation is bounded by the cited sources and accepted M23 r2
fabric. The 297-cell local-Arabian record represents documented fragmentation
without inventing precise tribal borders unsupported by the accepted geometry.
The checked Ottoman-Qara Qoyunlu frontier is a source-qualified shared-fabric
segment, not a newly digitized full frontier.

## Rollback note

Revert the shipping commit to remove the packet, generator, assets, tests, and
task records and restore the four-region provisional state. No public runtime
or deployment requires rollback.

## Next command

`$exec` — promote M49 region 015 (Northern Africa) with the next exact-date,
source-pinned four-layer packet.
