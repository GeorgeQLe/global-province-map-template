# M25C worldwide spatial-remediation re-research ship manifest

Date: `2026-08-22`
Status: **ready to ship**

## User goal

Conduct full evidence-first regional re-research of the 16 failed modern seams,
three non-executable seam controls, and 19 missing positive borders without
weakening the fail-closed M25C certification boundary or inventing historical
geometry.

## Changed files

- `tasks/m25c-spatial-remediation-reresearch.md`
- `tasks/m25c-spatial-remediation-reresearch-ship-manifest.md`
- `tasks/todo.md`
- `tasks/roadmap.md`
- `tasks/history.md`

## Per-file purpose

- `tasks/m25c-spatial-remediation-reresearch.md`: records the reproduced seam
  measurements, evidence dispositions for all 19 affected packets, exact
  recommended contracts, alternatives, QA impact, and reviewer decision.
- `tasks/m25c-spatial-remediation-reresearch-ship-manifest.md`: binds the
  research and synchronized project ledgers to this shipping boundary.
- `tasks/todo.md`: marks the read-only research gate complete and adds the
  decision-gated implementation and later final-review steps.
- `tasks/roadmap.md`: updates M25C's remaining gap from unspecified remediation
  to the exact pending decision bundle.
- `tasks/history.md`: records the session's evidence and non-mutation boundary.

## User-goal mapping

- All 19 seam assertion IDs, measurements, transition counts, and outcomes are
  enumerated in one baseline table.
- Every affected regional packet has a recommended seam treatment and
  positive-border disposition with source links where new evidence was used.
- The three non-executable controls are separated into true missing evidence
  (`039`) and covered no-transition observations (`057`, `061`).
- The recommendation identifies one promotion-ready border, one conditional
  priority digitization, five initial reviewed non-applicability cases, and the
  remaining evidence gaps without manufacturing a passing result.

## Tests run

- Fresh assembled-pass generation completed successfully before the research
  record was written.
- Ordinary pending-review QA executed against that candidate and produced the
  expected blocker inventory plus two incomplete-render errors: `56 errors`,
  `1 warning`. The seam results exactly matched the recorded 16 failures,
  three non-executable controls, and three passes.
- `git diff --check` passed with no whitespace errors.
- Full Python suite exercised all 421 tests. The sandboxed run produced
  `418 passed` and three localhost-bind permission failures. Re-running exactly
  those three server tests outside the network sandbox produced `3 passed` in
  `3.10s`; there are no test regressions or unresolved warnings.

## Skipped tests

- No render, browser, runtime compile, certification, publication, or deploy
  test is relevant to this documentation-only research gate. The research
  explicitly leaves pass bytes and all release permissions unchanged.
- No task-document audit ran because `scripts/audit-task-docs.mjs` is absent.

## Adversarial review

The review challenged six tempting false-positive paths: raising seam
tolerances, deleting negative controls, treating every zero-transition result
as a pass, restoring generated status borders, applying later maximum-extent
maps to 1444, and requiring territorial lines in disconnected or explicitly
non-cadastral community fabrics. Each is rejected in the research record.

The review also separated Southern Europe's all-unknown status sheet from the
two covered island controls, required both-side coverage diagnostics, retained
null/fail behavior for unknown or uncovered seams, scoped Portugal-Castile to
exclude changed segments, and kept Raichur conditional on a second dated
spatial source.

## Residual risk

- The recommendation is not an implementation and does not reduce the current
  54 post-render non-review blockers.
- Several linked historical sources describe zones, sites, or later periods;
  the record explicitly prevents their promotion without exact-date
  independent digitization and review.
- The proposed applicability and executable-zero contracts require schema,
  QA, fixture, and worldwide regression work after reviewer approval.

## Rollback note

Revert the documentation commit. Generated diagnostic candidates remain in
ignored `data/processed/` paths; no canonical, accepted-review, runtime,
certificate, demo, publication, or deployment artifact changed.

## Next command

After reviewer approval of the five-part decision bundle, run `$exec` to
implement the covered executable-zero contract, Southern Europe repair,
positive-border applicability records, and conditional digitization sequence.
